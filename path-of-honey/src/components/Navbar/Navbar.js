import React from 'react';
import { Link } from 'react-router-dom';
import useWidth from '../../hooks/useWidth';
import './Navbar.css';

const Navbar = () => {
    return (
        <header className="navbar-header">Navbar</header>
    )
}

export default Navbar;